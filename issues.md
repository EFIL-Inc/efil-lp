# EFIL LP QAレポート

対象: `C:\Users\t1120\dev\efil_lp_v1\index.html`
基準: `CLAUDE.md` の仕様・品質チェック・禁止事項

---

## チェック済み項目（問題なし）

- [x] 全9セクション実装済み（FV / 課題提起 / 原因 / 3ステップ / 強み / 事例 / 中間CTA / FAQ / 最終CTAフォーム）
- [x] CTAボタン3箇所実装済み（FV: L144 / 中間CTA: L357 / 最終フォーム送信: L459）。加えてヘッダー(L123)にも補助リンクあり。
- [x] FAQアコーディオンJS実装済み（L483-498）。クリックで開閉、aria-expanded切替、+/− アイコン切替あり。
- [x] フォームバリデーション実装済み（L501-526）。name/company/email必須＋email正規表現チェック。`novalidate`でネイティブを無効化しJS側で制御。
- [x] 見出し階層適切。h1は1つ（FV L133）、h2は各セクション見出し、h3はカード/リスト項目内で使用。
- [x] モバイルレスポンシブ対応済み。`viewport`設定、Tailwind `md:` ブレークポイント、グリッドが `grid-cols-1 md:grid-cols-3` 等に切替。
- [x] タイポグラフィ仕様準拠。h1=52px(`text-display`) / h2=36px(`text-h2`) / h3=18px(`text-h3`) / 本文=16px line-height 1.85。Noto Serif JPを見出しに適用(`h1,h2 { @apply font-serif }`)。
- [x] カラーパレット準拠。navy #0D1B2A / #1B2A4A、gold #C9A84C、白カード、ink 本文/補足。
- [x] セクション縦余白 120px（`py-section` = 120px）。
- [x] 本文最大幅 680px（`max-w-narrow`）。
- [x] 外部アイコンライブラリ不使用。チェックマークはSVG直書き（L165他）。
- [x] ヒーローに抽象グラフィック背景なし。タイポ主体。
- [x] SaaS青白配色ではなくダークネイビー×ゴールド基調。
- [x] アニメーションは scroll fade-in のみ（L108-113, L529-537）。
- [x] Google Fonts Noto Serif JP / Noto Sans JP をCDN読込（L10）。
- [x] 1ファイル完結。

---

## 指摘事項

### [High] メインコピーの引用符が仕様と不一致
- 該当: `index.html` L134
- 内容: 仕様書では `AIを"導入"するのではなく、"仕組み"にする。`（ASCIIの直線ダブルクォート `"`）と指定されているが、実装は `&ldquo;` / `&rdquo;`（左右カーリーダブルクォート “ ”）を使用。「一字一句正確」の要件に厳密には違反。
- 修正提案: 仕様書の表記をそのまま採用するなら `"導入"` `"仕組み"` の直線引用符に置換。もしくはタイポグラフィ優先でカーリーのまま運用する場合、仕様書側の表記ブレである旨を確認し合意を取る。サブコピー(L137)・セクション3見出し(L207)・footer(L475)・section3末文(L241)でも同様。

### [High] サブコピーの引用符が仕様と不一致
- 該当: `index.html` L137
- 内容: 仕様書 `"使ってみた"で、終わらせない。` に対し、実装は `&ldquo;使ってみた&rdquo;` でカーリー引用符。上記と同一問題。
- 修正提案: メインコピーと合わせて直線引用符に統一、もしくは仕様書更新の合意を取る。

### [Med] FAQ見出しの末尾句点が仕様と相違
- 該当: `index.html` L383
- 内容: 仕様 `Q. まず何から始めればいいかわかりません` に対し、実装は `Q. まず何から始めればいいかわかりません。` と句点付き。他のFAQ項目は仕様通り句点なしのため一貫性が崩れる。
- 修正提案: 末尾の「。」を削除し仕様に合わせる。

### [Med] FV「スクロールなしで全要素が収まること」要件のリスク
- 該当: `index.html` L128 FVセクション
- 内容: `min-h-screen`でビューポート高さを確保しているが、h1(52px×2行) + サブコピー(28px) + 補足文2行 + CTAで、小さなノートPC（高さ700px以下）では本文末尾やCTAが下にはみ出る可能性。特に日本語ブラウザ標準の line-height 1.25 だとh1だけで約130px、トータルで約480-520pxの描画高さになりギリギリ。
- 修正提案: 実機の一般的解像度（1366×768等）で目視確認。必要なら本文補足文を削るか、FV内の余白(`mt-10`, `mt-12`)を縮める。

### [Med] CTAボタンの box-shadow は無いが、focus:outline の扱い
- 該当: `index.html` L76 `.btn-cta`
- 内容: `focus:outline focus:outline-2 focus:outline-offset-2` はアクセシビリティ上望ましく、box-shadow禁止事項には抵触しないため問題なし。念のため記録。
- 修正提案: 対応不要。

### [Low] 事例セクション見出しが仕様と微妙に異なる
- 該当: `index.html` L327
- 内容: 仕様では事例セクションの見出し指定はなく、枠内テキスト「事例・実績は準備中です」のみ指定。実装は「導入から定着まで、伴走した事例。」という独自h2を追加。禁止ではないが、「プレースホルダー枠のみ実装」という仕様の簡素さから逸脱する懸念。
- 修正提案: 仕様に沿うなら見出し・導入文を簡素化、または削除。現状のままにする場合は仕様策定者に確認。

### [Low] `section` コンポーネントで `padding-top / padding-bottom ともに最低 120px` は満たしているが、一部 `section-dark py-section` の重複
- 該当: `index.html` L348
- 内容: 中間CTA `<section class="section-dark py-section">` で `.section` クラスを使わず `py-section` を直接付与。`.section` は `py-section` と等価なので機能的に問題ないが、クラス命名の一貫性を欠く。
- 修正提案: `class="section section-dark"` に統一。

### [Low] フォーム バリデーションのエラーメッセージ検索ロジック
- 該当: `index.html` L512 `input.parentElement.querySelector('.error-msg')`
- 内容: `input` の親要素(div)の中から `.error-msg` を探している。現状のDOM構造(L437-451)では正しく動作するが、将来DOMを変更すると壊れやすい。
- 修正提案: 対応不要（現状動作する）。堅牢性を上げるなら `id=${id}-error` のような明示参照へ変更。

### [Low] 課題提起セクションのアイコンSVGが「チェック」ではなく「レ点」でありコピペ重複
- 該当: `index.html` L165, L172, L179, L186
- 内容: 同一SVGが4箇所にインライン直書きで重複。コンポーネント化されていないため保守性が低いがサイズは軽微。禁止事項違反ではない。
- 修正提案: 対応不要。

---

## まとめ

- 機能面（セクション構成、JS動作、バリデーション、レスポンシブ、禁止事項）は概ね仕様準拠。
- High指摘はいずれも「引用符の種類」に起因。仕様書の表記通り直線ASCIIダブルクォートに修正するのが最も安全。
- Med以下は軽微な差異。
